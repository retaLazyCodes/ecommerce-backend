<style>
    .table {
        text-align: center;
    }

    .table td,
    .table th {
        vertical-align: middle;
    }

    h1 {
        color: #1c1cca;
    }

    hr {
        background-color: #ddd;
    }
</style>

<div>
    <h1>Lista de Productos</h1>
    <button class="btn btn-info mt-3" onclick="location.href = '/'">Volver al formulario</button>

    <br>
    <br>
    {{#if productos.length }}
    <div class="table-responsive">
        <table class="table table-dark">
            <tr>
                <th>Nombre</th>
                <th>Precio</th>
                <th>Foto</th>
            </tr>
            {{#each productos}}
            <tr>
                <td>{{this.title}}</td>
                <td>${{this.price}}</td>
                <td><img width="60" src={{this.thumbnail}} alt="not found"></td>
            </tr>
            {{/each}}
        </table>
    </div>
    {{else}}
    <h3 class="alert alert-warning">No se encontraron productos</h3>
    {{/if}}
</div>